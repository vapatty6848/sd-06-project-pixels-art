
window.onload = function () {
    
    backColorPalette = (["black", "blue", "brown", "red"]);
    //pega o array e adiciona as cores nos boxes
    function addColorInBoxes (colors) {
        let colorContainer = document.querySelector(".color_palette");
        
        for (let index in colors) {
          let divColors = colorContainer(colors[i]);
          colorContainer.appendChild(divColors);  
        }
    }
};
    function makeABoard () {
        for(var i=0; i>=)
    }